/**
 * The verified-taxa directory. ONE route.
 *
 * Data-only identities are listed here rather than each getting a page. Fifty
 * verified taxa are worth holding; fifty templated pages are not worth
 * publishing, and the crops validator fails if a data-only taxon acquires a URL.
 */
export const CROP_TAXA_PATH = '/crops/taxa';
