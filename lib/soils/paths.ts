/**
 * The soil-survey hub path.
 *
 * NOT `/soils`: that is the index of this corpus's soil-type reference pages —
 * chernozem, clay soil, and eighteen others — and those answer a different
 * question. `/soils` is "what is a chernozem"; this is "what does the survey
 * document about the ground in Iowa". Putting the second where the first lives
 * would have replaced twenty-one reference pages' index with a data table.
 */
export const SOIL_SURVEYS_PATH = '/soil-surveys';
